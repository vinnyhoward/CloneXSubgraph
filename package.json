{
  "name": "clonexsubgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ clonexsubgraph",
    "create-local": "graph create --node http://localhost:8020/ clonexsubgraph",
    "remove-local": "graph remove --node http://localhost:8020/ clonexsubgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 clonexsubgraph",
    "test": "graph test",
    "up": "docker compose up",
    "down": "docker compose down"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.62.0",
    "@graphprotocol/graph-ts": "0.31.0"
  },
  "devDependencies": {
    "matchstick-as": "0.6.0"
  }
}
